# Agent Workflow Rules

## Mandatory Final Checks After Any Code Change
- Run `npm run check`.
- If `check` fails, fix errors before finishing.

## Why
- `npm run lint` catches linting and style issues.
- `npm run build` validates TypeScript and production build integrity for Next.js.

## Notes for This Project
- This repository uses Next.js App Router.
